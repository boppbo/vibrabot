First run "pip install -e ." to install the project into path.
It will detect changes automatically and always run the current version
Afterwards you can execute "vibrabot-remoteapp.pyw" from any shell
